// Generated by CoffeeScript 1.12.2
(function() {
  var client, config, connectionString, pg, query;

  pg = require('pg');

  config = require('../config');

  connectionString = config.DATABASE_URL;

  client = new pg.Client(connectionString);

  client.connect();

  query = client.query('CREATE TABLE "report-snapshot-dates"(id SERIAL PRIMARY KEY, type VARCHAR(255), seller VARCHAR(255), "snapshot-date" DATE NOT NULL)');

  query.on('end', function() {
    return client.end();
  });

}).call(this);
